<Page x:Class="TiendaOga.Vistas.Clientes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Padrón de Clientes"
      Background="#F4F5F7"
      FontFamily="Segoe UI"
      d:DesignHeight="850" d:DesignWidth="1200">

    <Page.Resources>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#333333"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#4A4A4A"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#E8792D"/>
        <SolidColorBrush x:Key="AccentBrushHover" Color="#D96C22"/>
        <SolidColorBrush x:Key="RowAltBrush" Color="#FAFAFA"/>
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#FBFBFC"/>
        <SolidColorBrush x:Key="RowInactivoBrush" Color="#F8D7DA"/>

        <DropShadowEffect x:Key="CardShadow"
                          Color="#000000"
                          Opacity="0.06"
                          BlurRadius="12"
                          ShadowDepth="2"
                          Direction="270"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="FlatTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="{TemplateBinding Padding}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FlatTextBoxFocusable" TargetType="TextBox" BasedOn="{StaticResource FlatTextBox}">
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="18,9"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrushHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botón redondo para íconos de acción en la grilla (Modificar / Baja / Reactivar) -->
        <Style x:Key="IconGrillaButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="fondo" Background="Transparent" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="fondo" Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FlatColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource HeaderBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style x:Key="FlatRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="{StaticResource RowAltBrush}"/>
                </Trigger>
                <!-- Cliente dado de baja: la fila se pinta de rojo, sin importar la alternancia -->
                <DataTrigger Binding="{Binding Activo}" Value="False">
                    <Setter Property="Background" Value="{StaticResource RowInactivoBrush}"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFF3E9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1.2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Buscador + botón nuevo cliente -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,0,0">
                    <TextBlock Text="Buscar Cliente (por Nombre o DNI):" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="txtBuscar" Style="{StaticResource FlatTextBoxFocusable}" TextChanged="TxtBuscar_TextChanged"/>
                </StackPanel>
                <Button Grid.Column="2" Content="+ Nuevo Cliente" VerticalAlignment="Bottom" Height="34" Width="140" Style="{StaticResource PrimaryButton}" Click="BtnNuevoCliente_Click"/>
                <Button Grid.Column="4" Content="Limpiar" VerticalAlignment="Bottom" Height="34" Width="90" Style="{StaticResource PrimaryButton}" Click="BtnLimpiar_Click"/>
            </Grid>
        </Border>

        <!-- Grilla de clientes -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Listado de Clientes" Style="{StaticResource SectionTitleStyle}"/>

                <DataGrid x:Name="dgClientes"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#EFEFEF"
                          RowHeaderWidth="0"
                          BorderThickness="0"
                          Background="White"
                          AlternatingRowBackground="{StaticResource RowAltBrush}"
                          AlternationCount="2"
                          RowStyle="{StaticResource FlatRowStyle}"
                          ColumnHeaderStyle="{StaticResource FlatColumnHeader}"
                          FontSize="13"
                          RowHeight="40"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          SelectionChanged="DgClientes_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding IdCliente}" Width="50"/>
                        <DataGridTextColumn Header="Nombre/Razon social" Binding="{Binding NombreCompleto}" Width="*"/>
                        <DataGridTextColumn Header="DNI" Binding="{Binding Dni}" Width="120"/>
                        <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="130"/>
                        <DataGridTextColumn Header="Compras Realizadas" Binding="{Binding CantidadCompras}" Width="140"/>

                        <!-- Columna de Acciones: íconos de Modificar / Dar de baja / Reactivar -->
                        <DataGridTemplateColumn Header="Acciones" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

                                        <!-- Lápiz: Modificar -->
                                        <Button Click="BtnModificarCliente_Click" Tag="{Binding}"
                                                Style="{StaticResource IconGrillaButton}"
                                                ToolTip="Modificar cliente">
                                            <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="15" Foreground="#3182CE"/>
                                        </Button>

                                        <!-- Tacho / Reactivar: Dar de baja o Reactivar -->
                                        <Button Click="BtnToggleActivo_Click" Tag="{Binding}"
                                                Style="{StaticResource IconGrillaButton}"
                                                Margin="4,0,0,0">
                                            <Button.ToolTip>
                                                <ToolTip Content="Dar de baja">
                                                    <ToolTip.Style>
                                                        <Style TargetType="ToolTip">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Activo}" Value="False">
                                                                    <Setter Property="Content" Value="Reactivar cliente"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ToolTip.Style>
                                                </ToolTip>
                                            </Button.ToolTip>
                                            <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="15">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="&#xE74D;"/>
                                                        <Setter Property="Foreground" Value="#E53E3E"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Activo}" Value="False">
                                                                <Setter Property="Text" Value="&#xE777;"/>
                                                                <Setter Property="Foreground" Value="#38A169"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Button>

                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Detalle de compras del cliente seleccionado -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="lblHistorialTitulo" Grid.Row="0" Text="Historial de Compras (Seleccione un cliente para ver qué compró)" Style="{StaticResource SectionTitleStyle}"/>

                <DataGrid x:Name="dgCompras"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#EFEFEF"
                          RowHeaderWidth="0"
                          BorderThickness="0"
                          Background="White"
                          AlternatingRowBackground="{StaticResource RowAltBrush}"
                          AlternationCount="2"
                          RowStyle="{StaticResource FlatRowStyle}"
                          ColumnHeaderStyle="{StaticResource FlatColumnHeader}"
                          FontSize="13"
                          RowHeight="34">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Comprobante" Binding="{Binding NroComprobante}" Width="110"/>
                        <DataGridTextColumn Header="Fecha / Hora" Binding="{Binding Fecha}" Width="140"/>
                        <DataGridTextColumn Header="Artículos Comprados" Binding="{Binding DetalleProductos}" Width="*"/>
                        <DataGridTextColumn Header="Método de Pago" Binding="{Binding MetodoPago}" Width="120"/>
                        <DataGridTextColumn Header="Total Abonado ($)" Binding="{Binding Total, StringFormat={}{0:N2}}" Width="130"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>